/**
 * Placeholder — overwritten by `npm run generate` from `openapi.json`.
 *
 * The `paths` / `components` / `operations` types here are intentionally
 * permissive so `src/index.ts` still typechecks in a fresh clone before
 * the generator has run. CI regenerates this file on every build.
 */
export type paths = Record<string, unknown>;
export type components = Record<string, unknown>;
export type operations = Record<string, unknown>;
