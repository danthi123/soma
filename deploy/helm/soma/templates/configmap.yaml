apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "soma.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "soma.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
